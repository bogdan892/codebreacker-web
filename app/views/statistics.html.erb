<!DOCTYPE html>
<html lang="en" class="mdl-js">

<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <link href="assets/favicon.ico" rel="icon">
  <link href="assets/css/bootstrap.min.css" rel="stylesheet">
  <link href="assets/css/styles.css" rel="stylesheet">
  <script src="assets/js/jquery.min.js"></script>
  <script src="assets/js/bootstrap.min.js"></script>
  <title>Codebreaker Web</title>
</head>

<body>
  <section id="introduction">
    <div class="container">
      <div class="row fill-viewport align-items-center justify-content-center">
        <div class="col-12 col-md-6">
          <div class="row">
            <div class="col-md-auto">
              <h1 class="text-white title"><%=I18n.t(:codebreacker) %></h1>
            </div>
          </div>
          <p class="lead text-white-70"><%=I18n.t(:top_of_players) %></p>
          <hr class="mb-4">
          <div class="table-responsive-md scores">
            <table class="table table-striped table table-sm">
              <thead>
                <tr>
                  <th scope="col">#</th>
                  <th scope="col">Name</th>
                  <th scope="col">Level</th>
                  <th scope="col">Attempts Left</th>
                  <th scope="col">Hints Left</th>
                </tr>
              </thead>
              <tbody>
              <% load_from_db.each_with_index do |score, index| %>
              <%= " <tr><th scope='row'>#{index+1}</th>"%>
                <%= "<td>#{score[:name]}</td>"%>
                <%= "<td>#{score[:difficulty]}</td>"%>
                <%= "<td>#{score[:attempts_total] -score[:attempts_used]}/#{score[:attempts_total]}</td>"%>
                <%= "<td>#{score[:hints_total]-score[:hints_used]}/#{score[:hints_total]}</td>"%>
              <% end %>
              </tbody>
            </table>
          </div>
          <hr class="mb-4">
          <a class="btn btn-primary btn-lg" href="<%=HOME_URL %>" role="button">
            <%= I18n.t(:home_button) %>
          </a>
        </div>
       </div>
    </section>
  </body>
</html>
